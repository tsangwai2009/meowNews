<!--pages/detail/detail.wxml-->
<view class="swiper-tab">
    <view class="swiper-tab-item {{currentTab==0?'active':''}}" data-current="0" bindtap="clickTab">國內</view>
    <view class="swiper-tab-item {{currentTab==1?'active':''}}" data-current="1" bindtap="clickTab">國際</view>
    <view class="swiper-tab-item {{currentTab==2?'active':''}}" data-current="2" bindtap="clickTab">財經</view>
    <view class="swiper-tab-item {{currentTab==3?'active':''}}" data-current="3" bindtap="clickTab">娛樂</view>
    <view class="swiper-tab-item {{currentTab==4?'active':''}}" data-current="4" bindtap="clickTab">軍事</view>    
    <view class="swiper-tab-item {{currentTab==5?'active':''}}" data-current="5" bindtap="clickTab">體育</view>
    <view class="swiper-tab-item {{currentTab==6?'active':''}}" data-current="6" bindtap="clickTab">其他</view>
</view>

<swiper current="{{currentTab}}" duration="300"  bindchange="swiperTab" style="height: {{clientHeight?clientHeight+'px':'auto'}}; text-align: center;">
    <swiper-item>
      <scroll-view scroll-y style="height: {{clientHeight?clientHeight+'px':'auto'}}">
        <view class="news-item" wx:for="{{newsItem}}">
        <!-- <image class='news-photo' src='{{item.photo}}'></image> -->
          <view class="news-wrapper">
            <view class="news-time">{{item.time}}</view> 
            <view class="news-content">{{item.content}}</view> 
          </view>
        </view>
      </scroll-view>
    </swiper-item>
    <swiper-item>
      <scroll-view scroll-y style="height: {{clientHeight?clientHeight+'px':'auto'}}">
        <view class="news-item" wx:for="{{newsItem}}">
        <!-- <image class='news-photo' src='{{item.photo}}'></image> -->
          <view class="news-wrapper">
            <view class="news-time">{{item.time}}</view> 
            <view class="news-content">{{item.content}}</view> 
          </view>
        </view>
      </scroll-view>
    </swiper-item>
    <swiper-item>
      <scroll-view scroll-y style="height: {{clientHeight?clientHeight+'px':'auto'}}">
        <view class="news-item" wx:for="{{newsItem}}">
        <!-- <image class='news-photo' src='{{item.photo}}'></image> -->
          <view class="news-wrapper">
            <view class="news-time">{{item.time}}</view> 
            <view class="news-content">{{item.content}}</view> 
          </view>
        </view>
      </scroll-view>
    </swiper-item>
    <swiper-item>
      <scroll-view scroll-y style="height: {{clientHeight?clientHeight+'px':'auto'}}">
        <view class="news-item" wx:for="{{newsItem}}">
        <!-- <image class='news-photo' src='{{item.photo}}'></image> -->
          <view class="news-wrapper">
            <view class="news-time">{{item.time}}</view> 
            <view class="news-content">{{item.content}}</view> 
          </view>
        </view>
      </scroll-view>
    </swiper-item>
    <swiper-item>
      <scroll-view scroll-y style="height: {{clientHeight?clientHeight+'px':'auto'}}">
        <view class="news-item" wx:for="{{newsItem}}">
        <!-- <image class='news-photo' src='{{item.photo}}'></image> -->
          <view class="news-wrapper">
            <view class="news-time">{{item.time}}</view> 
            <view class="news-content">{{item.content}}</view> 
          </view>
        </view>
      </scroll-view>
    </swiper-item>
    <swiper-item>
      <scroll-view scroll-y style="height: {{clientHeight?clientHeight+'px':'auto'}}">
        <view class="news-item" wx:for="{{newsItem}}">
        <!-- <image class='news-photo' src='{{item.photo}}'></image> -->
          <view class="news-wrapper">
            <view class="news-time">{{item.time}}</view> 
            <view class="news-content">{{item.content}}</view> 
          </view>
        </view>
      </scroll-view>
    </swiper-item>
    <swiper-item>
      <scroll-view scroll-y style="height: {{clientHeight?clientHeight+'px':'auto'}}">
        <view class="news-item" wx:for="{{newsItem}}">
        <!-- <image class='news-photo' src='{{item.photo}}'></image> -->
          <view class="news-wrapper">
            <view class="news-time">{{item.time}}</view> 
            <view class="news-content">{{item.content}}</view> 
          </view>
        </view>
      </scroll-view>
    </swiper-item>
</swiper>

